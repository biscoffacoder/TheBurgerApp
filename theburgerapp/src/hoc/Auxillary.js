/**
 * This is a higher order component used to wrap the Layout FCE
 * so that we can use adjacent JSX syntax
 * @param {*} props 
 * @returns 
 */
const aux = (props)=>props.children;

export default aux;